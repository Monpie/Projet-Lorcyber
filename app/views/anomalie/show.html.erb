<h1>Test</h1>
<p>
<label>Statut de l'anomalie : </label>
  <%= @anomalie.statut %>
</p>
<p>

<p>
  <label>Descriptif de l'anomalie : </label>
  <%= @anomalie.descriptif %>
</p>

<p>
  <label>Date de l'anomalie : </label>
  <%= @anomalie.date %>
</p>

<% unless @anomalie.alerte_type.nil? %>
  <p><%=plan_type = PlanActionTypeController.new.generate(@anomalie.alerte_type)%></p>
  <% if plan_type == "Type = phishing" %>
  <p> test </p>
  <% end %>
  <% else %>

  <%= form_for :anomalie do |f| %>
      <% if @anomalie && @anomalie.errors.any? %>
        <h2><%= pluralize(@anomalie.errors.count, "erreurs") %> empêchent l'enregistrement de la société</h2>
        <ul>
          <% @anomalie.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
        </div>
      <% end %>
        Descriptif : <%=f.text_area(:descriptif, size:"50x6")%><br />
        <label>Alerte réel?</label>
      <%=  f.select :real_alerte, [['Oui', 1], ['Non', 2], ['Undefined', 3]]%>
  <%= f.submit %>
<% end %>

<!--
<%= form_tag "/anomalie/show", method: :post do %>
  <p>
    <input type="hidden" name="id" value="<%=@anomalie.id%>"/>
  <label>Alertes réelles? </label>
  <select name="realAlerte">
    <option value="yes">Oui</option>
    <option value="no">Non</option>
  </select>
  </p>

  <p>
    <label>Type de l'alerte : </label>
    <select name="type">
      <option value="phishing">Phishing</option>
      <option value="ddos">DDOS</option>
    </select>
  </p>
  <input type="submit" value="Valider" />
<% end %>
-->
<% end %>
